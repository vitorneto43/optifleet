# Core
SQLAlchemy>=2.0,<2.1
alembic>=1.13  # opcional, para migrações

# Servidor (produção/local)
gunicorn>=21.2   # usado na Render (Linux)
waitress>=3.0.0  # útil no Windows local, não atrapalha

# Já estavam no seu arquivo, mantenha:
Flask>=3.0
flask-login>=0.6
pandas
numpy
python-dotenv
requests
googlemaps
ortools
scikit-learn
folium
psycopg2-binary>=2.9


